metadata:
  labels:
    project: ${project.artifactId}
    version: ${project.version}
spec:
  ports:
  - name: web
    port: 80
    targetPort: 8082
  selector:
    project: ${project.artifactId}
    
